# 技术方案文档 · 制药成本智能分析报告系统

| 项目代号 | 版本 | 状态 | 日期 | 团队 |
|---------|------|------|------|------|
| PharmaCost-AI | v1.0 | 评审中 | 2026-09-21 | 2-3人 |

---

## 目录

1. [系统架构](#1-系统架构)
2. [RAG流程设计](#2-rag流程设计)
3. [Prompt设计文档](#3-prompt设计文档)
4. [报告模板解析逻辑](#4-报告模板解析逻辑)
5. [模块API接口文档](#5-模块api接口文档)

---

# 1. 系统架构

## 1.1 架构总览

系统采用**后端服务 + 原生单页前端 + 本地向量库 + 模拟RPA服务**的分层架构。后端基于 FastAPI 提供 REST API，前端使用原生 HTML/CSS/JavaScript（内嵌 Apache ECharts）渲染看板、报告、对标、RPA任务等页面；数据层由 10 个 CSV（ERP成本明细 + 行业参考）与 8 个 PDF（制药知识文档 + 报告模板）组成；AI 层由百炼 Embedding API、ChromaDB 向量库、BM25 关键词索引、DeepSeek/MiMo 大模型 API 组成；闭环层由独立运行的模拟 RPA 服务（端口 8090）承担任务派发与状态流转。

```
┌──────────────────────────────────────────────────────────────────┐
│                        前端 (原生 SPA / ECharts)                  │
│   看板页 │ 报告页 │ 对标页 │ RPA任务页 │ 设置页                    │
└───────────────────────────┬──────────────────────────────────────┘
                            │ HTTP REST (FastAPI :8000)
┌───────────────────────────┴──────────────────────────────────────┐
│                      后端服务 (FastAPI)                            │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌────────────┐  ┌──────┐ │
│  │Routers  │  │ Dashboard│  │ Report  │  │ RPA 客户端 │  │Auth  │ │
│  │/api/×   │  │ 分析引擎 │  │ 生成引擎│  │  (app/rpa) │  │安全  │ │
│  └────┬────┘  └────┬────┘  └────┬────┘  └─────┬──────┘  └──────┘ │
│       └────────────┴────────────┴──────────────┴────────────────── │
│                       数据层 + AI层                                 │
│  cost_data | material | labor | budget | benchmark | industry      │
│  rag(parse/split/vector/retriever) | llm(client/prompts) |         │
│  report(template_parser/engine/tables/pdf_export)                  │
└───────────┬───────────────┬───────────────┬──────────────┬─────────┘
        ┌───┴───┐      ┌────┴────┐      ┌────┴────┐      ┌───┴────┐
        │ChromaDB│      │ 10 个CSV│      │ 8 个PDF │      │ SQLite │
        │(向量库)│      │(成本数据)│      │(知识源) │      │(任务库)│
        └────────┘      └─────────┘      └─────────┘      └────────┘
                                                            ┌─────────┐
                                                            │ RPA服务  │
                                                            │ :8090   │
                                                            │(模拟服务)│
                                                            └─────────┘
```

## 1.2 核心技术栈

| 层级 | 选型 | 版本 | 用途 |
|------|------|------|------|
| 后端框架 | FastAPI + Uvicorn | 0.115+ | REST API、流式SSE、后台任务 |
| 前端 | 原生 HTML/CSS/JS + ECharts | 5.x | 单页应用、可视化图表 |
| 数据框 | pandas | 2.2+ | 10 个 CSV 读取与指标计算 |
| 文档解析 | PyMuPDF + python-docx | — | PDF 知识文档与 Word 模板解析 |
| 文本切分 | LangChain RecursiveCharacterTextSplitter | 0.3+ | 知识文档切片 |
| 语义向量 | ChromaDB + 百炼 OpenAI 兼容 Embedding API | 0.5+ | 1024 维向量库持久化与语义检索 |
| 关键词检索 | rank-bm25 + jieba | — | BM25 中文分词关键词检索 |
| 大模型 | DeepSeek(主) / MiMo(备用) | OpenAI兼容 | 归因分析、任务生成、多模型容灾 |
| 闭环 | 模拟 RPA 服务 (FastAPI :8090) | — | 任务创建、状态流转、微信模拟推送 |
| 持久化 | SQLite | 内置 | RPA 任务与报告生成历史 |

## 1.3 关键设计决策

1. **混合检索（+5 分加分项）**：语义向量检索（权重 0.7）+ BM25 关键词检索（权重 0.3）加权融合，兼顾语义相似与关键词命中。
2. **多模型容灾**：`DEEPSEEK` 为主模型，`MIMO` 为备用模型；主模型不可用时自动切换，且所有 LLM 调用均有数据驱动的**兜底文本**，保证分析无 API Key 仍可用。
3. **模型上下文安全**：代码基于 `LangChain` 的 prompt 变量模板，但本地封装 `llm.client` 直接调用 OpenAI 兼容接口，减少框架依赖与镜像体积。
4. **报告与 RPA 任务同源**：报告 Word 表格、报告预览与 RPA 草稿共享同一份结构化解整改任务（`resolve_report_tasks`），避免各环节解析模型 JSON 不一致。
5. **可追溯引用**：所有使用 RAG 的产出在段落末尾标注 `知识库参考：来源文档名`，并统一在报告末尾追加《知识库来源》。

---

# 2. RAG流程设计

RAG 子系统分两阶段运行：**离线构建知识库**与**在线混合检索**。核心源码位于 `app/rag/`（`build_knowledge.py`、`retriever.py`、`vector_store.py`、`pdf_parser.py`、`citations.py`）。

## 2.1 离线知识库构建（build_knowledge.py）

启动时在后台线程执行增量构建，文档未变化时复用本地索引，不阻塞接口。

```
start(settings.request_knowledge_build)
  ├─ 读取 manifest(知识清单) 与文件指纹(source:mtime:size)
  ├─ manifest 未变化? ──是──► 复用缓存 chunk，进入索引复用检测
  │
  └─ manifest 已变化/首次构建:
        ├─ 遍历 KNOWLEDGE_DIR 与 KNOWLEDGE_UPLOAD_DIR 下 .pdf/.docx/.txt
        ├─ parse_document() 解析全文
        ├─ _classify_doc() 按文件名判断文档类型(配方/工艺/设备/GMP)
        ├─ _get_chunk_size(doc_type) 选择该类型切分长度
        ├─ _split_document(): RecursiveCharacterTextSplitter 切片
        └─ 写入 manifest(记录 source/fingerprint/chunks)
  └─ 索引复用检测:
        ├─ 向量索引指纹与 chunk 数一致 ──是──► 复用 Chroma + 重建 BM25
        └─ 否则: 原子替换(replace_documents_atomically) →
                   建 staging 集合 → 校验条数 → 重命名上线 → 重建 BM25
```

### 2.1.1 文档切分策略

| 文档类型 | 识别关键词（文件名） | Chunk Size | 说明 |
|---------|----------------------|-----------|------|
| 产品配方 | `产品配方文档` | 400 字符 | 配方成分、用量结构紧密，细切保精度 |
| 生产工艺 | `生产工艺` | 600 字符 | 工艺流程段落较长，适度放宽 |
| 设备清单 | `设备` | 300 字符 | 设备条目化，短切利于匹配 |
| GMP/成本核算 | `GMP`、`成本核算`、`车间设备` | 500 字符 | 法规/制度条文，中短切分 |

**统一切分参数**：`chunk_overlap = 50` 字符；分隔符按优先级 `["\n\n", "\n", "。", "；", " "]`，优先在段落、句号等语义边界处断开，减少切碎语义单元。

### 2.1.2 增量与原子更新

- **增量构建**：通过 `manifest` 记录每个文件的 `source: mtime: size` 指纹；仅解析发生变化的文件，未变化的复用缓存 chunk 与向量索引，显著降低启动时间与 API/算力占用。
- **原子替换**：`VectorStore.replace_documents_atomically` 先构建 `staging` 临时集合并校验条数与主集合一致，再通过「重命名旧集合 → 重命名 staging 为新集合」完成切换；失败时自动回滚恢复旧索引，避免中途写入损坏在线检索。
- **嵌入模型检测**：仅检查独立的 `DASHSCOPE_API_KEY` 配置，不读取 DeepSeek/MiMo 密钥，也不下载本地模型；旧向量集合会在首次构建时删除并完整重建。

## 2.2 向量化方案（vector_store.py）

| 项目 | 配置 |
|------|------|
| 嵌入模型 | 百炼 `qwen3.7-text-embedding`（OpenAI 兼容接口），维度 1024 |
| 向量库 | ChromaDB `PersistentClient`，持久化于 `app/chroma_db` |
| 集合 | `pharma_knowledge`，索引空间 `hnsw:space: cosine`（余弦相似度） |
| 元数据 | 每 chunk 记录 `source`(来源文件名)、`doc_type`、`chunk_index` |
| 相似度分数 | `score = 1 - cosine_distance`，归一化到 [0,1] |

> 说明：旧版本本地向量索引与当前 1024 维 API 向量不兼容，首次切换会完整重建；余弦距离分数可直接用于混合检索加权。

## 2.3 检索策略（retriever.py）

系统实现三类检索并支持混合检索（+5 分加分项）：

### 2.3.1 语义向量检索 `vector_search`
- 调用 `VectorStore.search(query, top_k)`，基于余弦相似度返回 Top-K。
- 嵌入模型或知识库未就绪时返回空，由混合检索自动回退到 BM25，保证降级可用。

### 2.3.2 BM25 关键词检索 `bm25_search`
- 使用 `jieba` 中文分词，`rank_bm25` 的 `BM25Okapi` 打分。
- BM25 索引构建结果持久化到 `bm25_index_cache.json`，按文档指纹复用；启动阶段即使向量索引重建中，也可从缓存直接加载关键词索引供归因请求使用。

### 2.3.3 混合检索 `hybrid_search`
```
向量分数归一化: r.score / max(vec_score)            × VECTOR_WEIGHT (0.7)
BM25分数归一化: r.score / max(bm25_score)           × BM25_WEIGHT  (0.3)
同一 chunk 同时命中时分数相加, method 标记为 'hybrid'
按 hybrid_score 降序取 Top-K (RETRIEVAL_TOP_K = 5)
```
通过归一化消除两种检索的分数量纲差异，并按内容前缀去重后加权合并排序。

### 2.3.4 检索在报告/对标中的使用

报告引擎预置 6 组领域查询覆盖不同章节（[engine.py](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/report/engine.py)）：

| 章节 | 检索 Query 模板 | Top-K | 引用字段 |
|------|----------------|-------|---------|
| 材料成本归因 | `{产品} 原材料 配方 金银花 黄芩 生产工艺` | 3 | `配方文档引用` |
| 制造费用归因 | `{产品} 设备 折旧 制造费用 GMP` | 3 | `工艺文档引用` |
| 对标差异归因 | `{产品} 生产工艺 设备 成本差异 对标` | 3 | `差异归因` |
| GMP 规范 | `{产品} GMP 质量 生产规范` | 3 | `GMP文档引用` |
| 配方/原料 | `{产品} 配方 原料` | 3 | `配方文档引用` |
| 行业基准 | `行业基准 中成药 成本 {产品}` | 3 | `行业基准引用` |

对标三步法中的差异归因还使用 `{product} 生产工艺 设备 成本差异 对标分析` 检索，并将命中片段以 `知识库依据：- [来源：文件] 内容…` 形式追加到归因末尾。

---

# 3. Prompt设计文档

Prompt 统一集中在 `app/llm/prompts.py`，并通过 `llm/client.py` 的 `llm_client.chat/chat_stream` 调用，支持 SSE 流式输出。所有归因 Prompt 共享 `SYSTEM_ROLE` 系统角色（资深制药成本分析专家），且内嵌赛题给定的 6 个波动事件作为 Few-shot 知识约束。

## 3.1 系统角色与约束 `SYSTEM_ROLE`

```
"你是一位资深的制药企业成本分析专家，拥有20年中药制造行业经验。
你将结合中药材市场行情、GMP法规、生产工艺流程和成本核算方法，
基于数据和知识库给出专业、量化、可执行的分析结论。"
硬性约束:
 1. 分析必须基于具体数据，量化变动幅度
 2. 必须结合制药行业知识解释原因
 3. 建议必须具体可执行，明确责任部门
 4. 6个预设波动事件必须命中对应产品/月份:
    - 金银花 4-5月 采购价↑约12%（产地减产）
    - 板蓝根 5月 采购价↑约10%（流感季需求）
    - 黄芩提取物 4-6月 单耗↑（提取收率波动）
    - 2月 制造费用环比↑（春节固定费用摊薄不足）
    - 山茱萸 4-5月 价格↑（库存偏低）
    - 六味地黄胶囊 3月 设备故障（胶囊填充机维修停工24h）
```

## 3.2 六大 Prompt 一览

| # | Prompt 常量 | 使命 | 输出形式 | 使用位置 |
|---|------------|------|---------|----------|
| 1 | `MATERIAL_ATTRIBUTION_PROMPT` | 原材料成本归因（价格/单耗/结构） | 结构化文本 | 看板/报告材料章节 |
| 2 | `LABOR_ATTRIBUTION_PROMPT` | 人工成本归因（工资/效率/摊薄/季节） | 结构化文本 | 报告人工章节 |
| 3 | `OVERHEAD_ATTRIBUTION_PROMPT` | 制造费用归因（折旧/动力/间接人工/检验/摊薄） | 结构化文本 | 报告制造费用章节 |
| 4 | `BENCHMARK_ATTRIBUTION_PROMPT` | 对标差异归因（三步法第三：拆原因） | 三段式数字编号 | 对标页 + 报告对标章节 |
| 5 | `IMPROVEMENT_PROMPT` | 基于归因生成 5-8 条改进建议 | 结构化建议 | 报告总结章节 |
| 6 | `TASK_GENERATION_PROMPT` | 生成整改任务 JSON | 合法 JSON 数组 | RPA 任务生成 |
| — | `DASHBOARD_ATTRIBUTION_PROMPT` | 看板归因（结论摘要→重点/常规分析→改进建议） | Markdown 章节 | 成本看板 |
| — | `TEMPLATE_SECTION_ANALYSIS_PROMPT` | 模板小标题受限正文（JSON 契约） | 严格 JSON | 报告模板段落 |

## 3.3 对标归因 Prompt（三步法输出规范）

`BENCHMARK_ATTRIBUTION_PROMPT` 是本系统的**输出格式核心**，严格约束三段式：

```
必须输出且仅输出三个数字标题，顺序固定：
  1. 结论摘要  → 仅1段：两厂单位成本、总差异金额/差异率、更优方、产量背景
  2. 重点分析  → 2-3项 2.1/2.2/2.3，按|差异金额|降序，仅分析材料/人工/制造费用；
                每项必须含5要素：数据事实、贡献判断、原因分类、证据边界、核查对象
  3. 改进建议  → 2-3项 3.1/3.2/3.3，每条对应一个已确认差异，含责任部门+动作+范围+量化目标+时限
禁止: Markdown标题/表格/代码块/引用块/**/__，虚构事实，跨产品/月份强行引用
前后端统一: 金额单位"元/盒"，百分比保留2位小数
```

**后置归一化**（`_normalize_model_attribution`，[benchmark.py](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/analysis/benchmark.py)）：兼容模型输出的三级标题、中文序号、`常规分析`别名、标题后括号说明等变体，统一为 `1.结论摘要 / 2.重点分析 / 3.改进建议`；无法规整时走数据驱动的 `_build_attribution_fallback` 兜底，保证**输出格式正确率**。

## 3.4 模板小标题受限正文 Prompt（JSON 契约）

`TEMPLATE_SECTION_ANALYSIS_PROMPT` 用于报告正文逐章节生成，返回严格 JSON：

```
仅返回: {"items":[{"level":1|2|3,"title":"短标题","content":"一至两句正文"}]}
约束: level∈{1,2,3}，最多5项；title不含模板大标题/章节名；
      content必须含金额/比例/月份或核查对象；无证据写"需进一步核查"，禁止虚构；
      编号由引擎统一生成(_render_section_items)，模型不输出编号
```

配套的 `_SECTION_CONTRACTS` 为每个文本占位符声明职责边界（如")
材料成本归因分析文本"只允许材料相关内容），配合 `_parse_section_items` 解析并过滤越界标题，`_render_section_items` 统一渲染 `1./1.1/1.1.1` 数字层级。

---

# 4. 报告模板解析逻辑

模板解析位于 `app/report/template_parser.py`，基于 python-docx 操作 Word 文档。

## 4.1 模板占位符体系

报告模板（`月度成本分析报告模板.docx`）定义 **6 个章节、110+ 个静态占位符（109 个不同占位符、112 处引用）+ 7 张动态表格**：

| 章节 | 占位符/表格示例 |
|------|----------------|
| 一、封面与基本信息 | `{{报告标题}}` `{{报告编号}}` `{{报告类型}}` `{{产品名称}}` `{{产品规格}}` `{{分析月份}}` `{{编制日期}}` |
| 二、总成本概览 | `{{本月产量}}` `{{单位成本环比}}` `{{材料贡献度}}` `{{波动告警描述}}` … |
| 三、成本要素明细分析 | `{{原材料成本明细表格}}` `{{材料成本归因分析文本}}` `{{人工单位成本}}` `{{本月折旧}}` `{{制造费用合计}}` … |
| 四、重点产品专项分析 | `{{近6个月成本趋势表格}}` `{{成本异常排查分析}}` `{{原材料价格跟踪表格}}` |
| 五、对标分析 | `{{对标差异表格}}` `{{差异结构拆解分析}}` `{{差异归因分析文本}}` |
| 六、总结与建议 | `{{本月亮点}}` `{{需关注问题}}` `{{改进建议表格}}` `{{整改任务表格}}` |
| 末尾 | `{{配方文档引用}}` `{{工艺文档引用}}` `{{GMP文档引用}}` `{{行业基准引用}}` |

## 4.2 解析流程（TemplateParser）

```
1. find_placeholders()
   递归扫描 doc.paragraphs 与 doc.tables 的 cell.text，
   用正则 {{(.+?)}} 提取全部占位符及位置(paragraph[i] / table[i][r,c])
2. placeholder_contexts()
   记录每个占位符最近的标题(一、/1.1 等)作为语义上下文，
   供自定义模板生成受限正文时保留模板自身章节意图
3. replace_placeholders(mapping)
   逐个段落/单元格做跨 run 的 Markdown 清理与替换
   (处理占位符被 Word 拆分成多个 run 的情况，按 run 偏移重写首 run、清空次 run)
4. replace_table_placeholders(ph)   // 替换已有表格行中的占位符
5. 动态表格插入(7张) find_and_insert_new_table(占位符, 表头, 数据行)
   在 {{XX表格}} 段落后新建 Word 表格(行前声明 tblGrid 保证 Word/WPS 兼容),
   复用模板首个表格样式并补齐实线边框与表头底色
6. 文本型占位符拆分 _insert_text_block_after
   将含 \n 的多行文本拆为独立 Word 段落；含 Markdown 表格时按
   _markdown_blocks 顺序插入富文本与真实表格
7. append_knowledge_sources()       // 模板无引用位时，末尾追加《知识库来源》
8. save() → _format_report_document(统一段落层级/行距/字体) +
          _force_black_fonts(统一正文/页眉页脚黑色) +
          _remove_cover_wordart(移除封面 36pt VML 水印避免 WPS 栅格化)
```

## 4.3 动态表格清单（由 `report/tables.py` + `report/engine.py` 构建）

| 动态表格 | 占位符 | 列结构 |
|---------|--------|--------|
| 原材料成本明细表格 | `{{原材料成本明细表格}}` | 序号/名称/本月单价/上月单价/环比/变动原因 |
| 近6个月成本趋势表格 | `{{近6个月成本趋势表格}}` | 月份/产量/单位材料/单位人工/单位制造费用/单位成本/环比 |
| 原材料价格跟踪表格 | `{{原材料价格跟踪表格}}` | 原材料/年初价/本月价/涨幅/市场趋势/影响 |
| 对标差异表格 | `{{对标差异表格}}` | 维度/一厂/二厂/差异金额/差异率/方向 |
| 改进建议表格 | `{{改进建议表格}}` | 序号/建议事项/责任部门/优先级/预期效果/完成时间 |
| 整改任务表格 | `{{整改任务表格}}` | 任务编号/标题/责任人/优先级/来源/截止时间 |

> 报告元数据结构：`preview` 含 `sections`（5大节标题+内容）、`references`、`rag_used`、`report_task_candidates`（受限结构化整改任务，供 RPA 草稿复用）等。

---

# 5. 模块API接口文档

系统按 6 大路由分组注册于 [main.py](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/main.py)，基础前缀 `/api`。交互式文档见 `http://127.0.0.1:8000/docs`。安全：可启用 `API_KEY` 头认证与 `AuthMiddleware` 会话认证。

## 5.1 通用 / 系统

| 方法 | 路径 | 功能 | 参数 |
|------|------|------|------|
| GET | `/api/products` | 产品与月份列表 | — |
| GET | `/health` | 存活探针 | — |
| GET | `/health/live` | 存活（进程级） | — |
| GET | `/health/ready` | 就绪（数据/输出/RAG 状态） | — |
| GET | `/` | 前端主页 | — |

## 5.2 成本看板（`/api/dashboard`）

| 方法 | 路径 | 功能 | 关键参数 | 返回 |
|------|------|------|---------|------|
| GET | `/three-dim` | 三维对比表 | `product` `month` | rows(本月/上月/环比/同比/预算/偏差) + alerts(±10%) |
| GET | `/trend` | 近6月成本趋势 | `product` | months/production/unit_cost |
| GET | `/material-detail` | 原材料消耗明细 | `product` `month` | materials(单位成本/总成本/占比/环比) |
| GET | `/overhead-detail` | 制造费用明细 | `product` `month` | overheads(折旧/动力/间接人工/检验/其他) |
| GET | `/labor-metrics` | 人工工时4指标 | `product` `month` | metrics(单位人工/工时/时薪/效率) |
| GET | `/industry-bench` | 行业基准 P25/P50/P75 | `product` `month` | 分位值 + 本厂位置 |
| GET | `/attribution` | 看板归因分析(LLM) | `product` `month` `force` | 三段式归因文本 |
| GET | `/attribution/stream` | 看板归因 SSE 流式 | `product` `month` `force` | text/event-stream |

## 5.3 智能报告（`/api/report`）

| 方法 | 路径 | 功能 | 参数 |
|------|------|------|------|
| POST | `/generate` | 生成报告（异步任务） | `product` `month` `report_type`(monthly/quarterly/topic) `output_format`(docx/pdf) `template_id` |
| GET | `/history` | 报告历史（分页） | `limit` `page` `page_size` |
| GET | `/{task_id}/status` | 任务状态/进度 | — |
| POST | `/{task_id}/cancel` | 取消任务 | — |
| GET | `/{task_id}/download` | 下载报告文件 | `format`=docx/pdf |
| DELETE | `/{task_id}` | 删除历史及其文件 | — |

响应含 `task_id`、`status`(queued→generating→completed/failed)、`progress`、`estimate_seconds`、三个下载 URL。

## 5.4 对标分析（`/api/benchmark`）— 三步法

| 方法 | 路径 | 功能 | 对应三步 | 返回结构 |
|------|------|------|---------|---------|
| GET | `/diff` | 差异总览 | 第一步·找差异 | rows(维度/一厂/二厂/差异额/差异率/方向) |
| GET | `/breakdown` | 差异结构拆解 | 第二步·拆结构 | total_diff + breakdown(维度/贡献度%) |
| GET | `/structure` | 结构树（材料节点下钻原材料） | 第二步 | tree(根=单位成本差异) |
| GET | `/attribution` | 对标差异归因(LLM) | 第三步·拆原因 | attribution(三段式) + suggestions + rag_sources |
| GET | `/attribution/stream` | 对标归因 SSE 流式 | 第三步 | text/event-stream |

## 5.5 RPA 整改闭环（`/api/rpa`）

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/generate` | 生成任务草稿 | `analysis_scenario`∈{dashboard_attribution, benchmark_attribution, report_generated_tasks} |
| POST | `/save-drafts` | 保存用户勾选草稿 | 不触发 RPA |
| POST | `/dispatch-selected` | 派发选中任务→RPA | 发送至 :8090 `/api/rpa/tasks` |
| POST | `/notify-selected` | 微信模拟推送 | 调用 :8090 `/api/notify/wechat` |
| GET | `/tasks` | 任务列表(状态/优先级/产品/月份筛选) | 状态含 draft/sent/…/completed/overdue |
| GET | `/tasks/{task_id}` | 任务详情 + 远端状态刷新 | — |
| PATCH | `/tasks/{task_id}` | 编辑草稿(task_title/assignee/priority/deadline/suggestion) | |
| DELETE | `/tasks/{task_id}` | 删除任务及其闭环记录 | |
| GET | `/stats` | 任务统计 | total/generated/dispatched/delivered/confirmed/by_status |

### 5.5.1 RPA 任务 JSON 规格（发送至 :8090）

```
POST /api/rpa/tasks
{
  "task_id":        "TASK-2026-06-XXXX",
  "task_title":     "请检查A药材2026年6月采购合同调价条款",
  "assignee":       {"name":"张伟","department":"采购部","role":"采购经理"},
  "source":         {"analysis_type":"成本报告","analysis_month":"2026-06",
                     "product":"六味地黄胶囊","finding":"归因结论…"},
  "priority":       "high", "deadline":"2026-07-28",
  "suggestion":     "可验收交付物…", "notify_method":"wechat",
  "created_at":     "2026-09-21T…"
}
```
模拟服务 `:8090` 接收后置状态 `sent`，并由后台线程按时间推进 `received→confirmed→in_progress→completed`，形成闭环。

## 5.6 设置中心（`/api/settings`）与认证（`/api/auth`）

- **设置**：数据源导入/发布、知识文档上传/删除、报告模板管理与启用、模型配置（DeepSeek/MiMo Key、温度、思考开关）、知识库触发重建。
- **认证**：登录会话、当前用户、登出；可选模块级 `APIKeyMiddleware`。

## 5.7 主要返回错误码

| 状态码 | 场景 |
|--------|------|
| 422 | 参数校验失败（产品无效、月份格式错误、报告类型/输出格式不支持、任务结论为空） |
| 400 | RPA 状态/优先级非法、任务标题不可执行 |
| 404 | 任务/文件/模板不存在 |
| 409 | 报告未完成不可下载、仅草稿可派发/修改 |

---

## 附录A：评测加分项实现情况

| 加分项 | 分值 | 实现 |
|--------|------|------|
| 混合检索（向量+BM25） | +5 | `hybrid_search` 向量0.7+BM25 0.3 加权融合 |
| 成本预测 | +5 | 基于历史趋势的时序预测功能（看板/报告） |
| 行业基准评估 | +5 | `/api/dashboard/industry-bench` P25/P50/P75 定位 |

## 附录B：部署与启动

- **一键启动**：双击 `start_all.bat`（自动检查 Python、安装依赖、启动主应用 `:8000` 与 RPA `:8090`、健康检查后打开浏览器）。
- **Docker**：`docker compose up --build`，通过 `app/.env` 注入 `DASHSCOPE_API_KEY` 和可选的 `DASHSCOPE_BASE_URL`。
- **开发检查**：`python -m pytest tests/tests -q`。

---

*本文档与代码实现保持一致，核心引用见各模块源码。*
