"""6个Prompt模板 — Few-shot策略"""

SYSTEM_ROLE = """你是一位资深的制药企业成本分析专家，拥有20年中药制造行业经验。
你熟悉中药材市场行情、GMP法规、生产工艺流程和成本核算方法。
请基于提供的数据和知识库检索结果，给出专业、量化、可执行的分析结论。

重要要求：
1. 分析必须基于具体数据，量化变动幅度
2. 必须结合制药行业知识解释原因
3. 建议必须具体可执行，明确责任部门
4. 以下6个已知波动事件必须在归因中明确提及（如涉及对应产品和月份）:
   - 金银花4-5月采购价上涨约12%（产地减产）
   - 板蓝根5月采购价上涨约10%（流感季需求增加）
   - 黄芩提取物4-6月单耗上升（提取工艺收率波动）
   - 2月制造费用环比上升（春节固定费用摊薄不足）
   - 山茱萸4-5月价格上涨（库存偏低）
   - 六味地黄胶囊3月设备故障（胶囊填充机维修导致停工24h）
"""

# 模板正文分析。编号由报告引擎统一生成，模型只返回受限的结构化内容，
# 避免把一篇综合报告错误地填进某一个模板小标题。
TEMPLATE_SECTION_ANALYSIS_PROMPT = """请为制药成本报告中的一个模板小标题生成正文。

产品：{product}
月份：{month}
占位符：{placeholder}
模板小标题：{section_title}
本节职责：{section_purpose}

数据：
{context}

知识库参考：
{rag_context}

严格要求：
1. 只能回答“本节职责”范围内的内容，不得复述模板大标题，不得输出其他章节、报告开场白、总结性全文或任务清单。
2. 只能把数据或知识库直接支持的内容写成事实；原因没有直接证据时必须写“需进一步核查”，不得虚构供应商、设备故障、工艺调整、加班或季节性事件。
3. 返回且仅返回 JSON，不要 Markdown、代码围栏或编号。格式为：
   {{"items":[{{"level":1,"title":"短标题","content":"一至两句正文"}},{{"level":2,"title":"短标题","content":"一至两句正文"}}]}}
4. level 只能为 1、2 或 3；最多 5 项；title 不得包含数字编号、模板标题或“结论摘要/重点分析/改进建议”等无关章节名。
5. 每项正文应包含必要的金额、比例、月份或核查对象；没有足够数据时明确说明数据缺口。
"""

# Prompt 1: 材料成本归因
MATERIAL_ATTRIBUTION_PROMPT = """请分析以下产品的原材料成本变动原因。

## 数据
{context}

## 市场行情参考
{market_info}

## 相关知识
{rag_context}

请从以下角度分析：
1. 价格因素：哪些原材料采购价变动显著？与市场行情是否一致？
2. 消耗因素：单耗是否有变化？可能的工艺原因？
3. 结构因素：高价值原材料占比变化？

请以结构化文本输出，每点包含具体数据。
"""

# Prompt 2: 人工成本归因
LABOR_ATTRIBUTION_PROMPT = """请分析以下产品的人工成本变动原因。

## 数据
{context}

请从以下角度分析：
1. 工资水平变化（平均小时工资）
2. 效率变化（人工工时、人工效率）
3. 产量摊薄效应
4. 加班/季节性因素

请结合生产日历分析（2月春节、5月旺季等），以结构化文本输出。
"""

# Prompt 3: 制造费用归因
DASHBOARD_ATTRIBUTION_PROMPT = """请基于以下成本看板JSON，生成可直接放入正式月度成本分析报告的归因分析段落。

产品：{product}
月份：{month}
看板数据：
{context}

知识库参考（仅重点波动时提供）：
{rag_context}

要求：
1. 先概述单位成本的环比变动额和变动率。
2. 按贡献度从高到低分析直接材料、直接人工和制造费用，必须引用具体金额和百分比。
3. 对触发阈值的项目单独标记为重点分析。重点分析应以你的具体归因判断为主体，结合成本要素贡献度、物料/费用明细、产量规模和知识库证据，形成连贯的分析段落；不要机械套用“数据事实、明细证据、影响判断、核查路径、处置动作”等固定小标题，也不要重复同一段免责声明。需要时自然写出本月、上月、变动额、变动率、贡献度、原因证据、证据边界和下一步核查动作。重点分析不得少于常规分析的篇幅；没有证据时明确写“需进一步核查”，不得编造事实。
4. 给出至少两条可执行建议，明确采购、生产、设备或财务责任部门、具体动作、交付物和完成时限。
5. 输出必须严格按以下顺序且每个章节只出现一次：`## 结论摘要` → 有阈值告警时的 `## 重点分析`（无告警时为 `## 常规分析`）→ `## 改进建议`。重点分析要围绕数据证据、贡献度和明细原因展开，不得重复同一段通用免责声明；不要在正文中再次输出知识库来源章节。
6. 使用正式报告语气，需要对比多个成本要素时可使用标准 Markdown 表格，必须包含表头、下一行 `|---|---|` 分隔线和完整数据行。请使用 Markdown `**重点内容**` 标注关键驱动因素、异常项和必须执行的动作，系统会保留并以加粗形式展示。不要输出JSON或免责声明。
7. 当提供了知识库参考时，只能引用与本次波动相关的内容；使用知识库结论时在句末标注 `[来源：文件名]`，没有知识库参考时不要添加来源标注。

"""

OVERHEAD_ATTRIBUTION_PROMPT = """请分析以下产品的制造费用变动原因。

## 数据
{context}

## 设备相关知识
{rag_context}

请从以下角度分析：
1. 折旧费：设备更新/产能变化
2. 动力费：能耗变化
3. 间接人工：人员变化
4. 检验费：批次/频次变化
5. 固定费用摊薄：产量变化的影响

请以结构化文本输出。
"""

# Prompt 4: 对标差异归因
BENCHMARK_ATTRIBUTION_PROMPT = """请分析中药一厂与中药二厂的成本差异原因。

## 对标数据
{context}

## 相关知识
{rag_context}

严格遵循以下输出规则：
1. 仅输出以下三个数字标题，标题文字必须保持不变且按此顺序出现：`1. 结论摘要`、`2. 重点分析`、`3. 改进建议`。不要添加“常规分析”“总体结论”“数据来源”等其他章节标题，也不要在三个章节之外输出内容。
2. 禁止输出 Markdown 标题、表格、代码块、引用块、HTML，以及 `**`、`__` 或反引号。所有内容使用普通段落和数字编号。
3. `1. 结论摘要`：只写 1 段，按以下顺序说明：
   - 单位成本：分别写出一厂和二厂单位成本；
   - 总差异：写出一厂减二厂的差异金额和差异率，并明确“中药一厂更优/中药二厂更优/基本持平”；
   - 规模背景：引用两厂产量及产量差异；如总成本方向与单位成本不同，必须说明总成本受产量影响，不能据此判断成本控制优劣。
4. `2. 重点分析`：必须列出 2-3 个二级编号项，格式为 `2.1`、`2.2`、`2.3`，严格按 `|差异金额|` 从大到小排序，只分析直接材料、直接人工、制造费用，不分析产量和总成本。每个列表项必须包含以下 5 个信息：
   - 数据事实：一厂值、二厂值、差异金额、差异率；
   - 贡献判断：说明该要素是主要/次要/较小差异，必要时引用贡献度；
   - 原因分类：只能从价格、单耗/收率、人工效率、产量摊薄、能耗、维修或费用分摊中选择与数据相符的类别；
   - 证据边界：原因必须来自“对标数据”或“相关知识”，没有直接证据时必须写“需进一步核查”，不得把可能性写成事实；
   - 核查对象：明确应核对的原始记录，例如采购入库价、领料单、投料量、提取收率、工时、人员配置、设备运行记录、能耗或费用分摊表。
5. 原因表述必须区分事实和假设：使用“数据显示/知识库指出”描述已证实内容，使用“可能/需进一步核查”描述待验证原因。严禁凭空推测供应商、药材产地、配方标准、自动化设备、管理制度或具体故障；知识库没有与本产品和月份直接相关的证据时，不得强行引用其他产品或月份事件。
6. `3. 改进建议`：必须列出 2-3 条二级编号列表（使用 `3.1`、`3.2`、`3.3`），每条只对应一个已确认的重点差异，并同时写清：责任部门、核查或改善动作、核查数据范围、量化目标（例如将单位差异缩小至某金额或某百分比）和完成时限。没有数据支持时，目标写“完成差异核查并提交改善方案”，不得编造节约金额。
7. 建议不得重复重点分析原文，不得生成与本次差异无关的任务；不要在本节后再次输出“改进建议”或任务清单。
8. 输出前自检：必须恰好存在三个章节；摘要为 1 段；重点分析为 2-3 条；建议为 2-3 条；所有金额单位统一为“元/盒”，百分比保留 2 位小数。
"""

# Prompt 5: 改进建议生成
IMPROVEMENT_PROMPT = """基于以下成本分析结论，请生成具体的改进建议。

## 分析结论
{context}

## GMP相关要求
{rag_context}

请生成5-8条改进建议，每条包含：
1. 建议事项（具体做什么）
2. 责任部门（采购/生产/设备/财务等）
3. 优先级（high/medium/low）
4. 预期效果（量化的成本节约）
5. 建议完成时间
"""

# Prompt 6: 整改任务JSON生成
TASK_GENERATION_PROMPT = """请根据以下分析场景和归因结论，生成结构化的RPA整改任务JSON。

## 分析结论
{context}

## 产品信息
产品: {product}, 月份: {month}

请只输出JSON数组。每个任务必须严格包含以下字段：
{{
    "task_title": "请检查A药材2026年6月采购合同调价条款",
    "assignee": {{"name": "责任人姓名", "department": "部门", "role": "岗位"}},
    "source": {{
        "analysis_scenario": "关联的分析场景",
        "attribution_conclusion": "关联的归因结论"
    }},
    "priority": "high/medium/low",
    "deadline": "YYYY-MM-DD",
    "expected_result": "可验收的交付物，例如：输出采购入库价与合同单价差异核查表，并提交整改措施"
}}

注意：
- 每项任务标题必须同时写明可执行动作和核查对象，控制在15-50字，例如“复核金银花采购入库价与合同单价差异”；不得输出“加强管理”“持续关注”“优化成本”“提升意识”等口号式任务
- expected_result 必须明确任务完成后提交什么可验收的记录、差异表或改善方案，不能写“完成整改”“持续跟进”等空泛表述
- 任务只能依据输入的归因结论和分析证据生成，不能虚构供应商、设备故障、节约金额或业务事实
- task_title必须包含采购单价、合同、工时、领料单、单耗、设备运行记录、能耗或费用分摊等至少一个具体对象
- 不要生成task_id、status、产品或月份字段，这些由系统负责补齐
- assignee必须同时包含name、department、role；部门和岗位必须与任务匹配
- source必须同时包含analysis_scenario和attribution_conclusion，且必须复述输入结论中的关键事实
- deadline在分析月份之后1个月内
- 优先级根据影响程度确定
- 请直接输出JSON数组，不要额外解释
"""
