# 前端UI需求文档 · 制药成本智能分析报告系统

| 项目代号 | 文档版本 | 设计风格 | 创建日期 |
|---------|---------|---------|---------|
| PharmaCost-AI | v2.6 | 工业制药GMP控制室 / ERP风格 | 2026-08-26 |

---

## 目录

1. [项目概述与目标](#1-项目概述与目标)
2. [用户角色与场景分析](#2-用户角色与场景分析)
3. [信息架构与页面结构](#3-信息架构与页面结构)
4. [视觉设计规范](#4-视觉设计规范)
5. [交互设计规范](#5-交互设计规范)
6. [组件库规范（状态矩阵）](#6-组件库规范状态矩阵)
7. [响应式与性能要求](#7-响应式与性能要求)
8. [无障碍与可访问性设计](#8-无障碍与可访问性设计)
9. [图标系统规范](#9-图标系统规范)
10. [前端技术与实现约束](#10-前端技术与实现约束)
11. [设计交付物清单](#11-设计交付物清单)

---

## 1. 项目概述与目标

### 1.1 项目定位

面向制药企业财务分析与生产管控场景的**成本智能分析看板系统**。将原本3-5天的人工成本分析压缩至5分钟内完成，输出结构化Word报告、可视化对标分析与RPA整改任务闭环。

### 1.2 UI设计核心原则（**消除AI感**）

| 原则 | 具体要求 | 禁止项 |
|------|---------|-------|
| **方正克制** | 采用工业级小半径圆角（2-4px），方形分割线，倒角无弧度 | ❌ 大圆角（>8px）、胶囊形按钮、圆形卡片 |
| **蓝灰工业配色** | 主色采用GMP控制室深蓝灰，强调色为工业橙（仅告警和标识用） | ❌ 马卡龙色、渐变色块、玻璃拟态（Glassmorphism） |
| **数据密度优先** | 减少留白，表格和指标卡优先保证信息密度 | ❌ 超大留白、过度呼吸感、装饰性空白区域 |
| **无装饰主义** | 去除所有非功能性视觉元素，仅保留分割线和数据标识 | ❌ 柔和阴影、模糊背景、几何装饰图形 |
| **ERP原生质感** | 复刻SAP/Oracle/用友等传统ERP界面的下拉、表格、面包屑风格 | ❌ AI式插画、emoji图标、动画背景 |

### 1.3 设计目标量化

- 首屏可展示**≥4个指标卡 + 1张数据表 + 1张图表**（1920×1080分辨率）
- 表格单屏显示**≥14行数据**（不含表头）
- 页面加载到首屏可交互时间 ≤**1.5秒**
- SVG图标加载零外部依赖，无emoji残留（DOM检测`hasEmoji:false`）

---

## 2. 用户角色与场景分析

### 2.1 用户角色矩阵

| 角色 | 主要页面 | 典型场景 | 核心关注点 |
|------|---------|---------|-----------|
| **P1 成本会计 / 财务分析师** | 成本看板、智能报告 | 每月1-5日完成上月成本分析，从看板定位异常，一键生成Word报告提交财务经理 | 数据准确性、偏差定位速度、报告结构完整性、导出功能 |
| **P2 财务经理 / 生产总监** | 成本看板、对标分析 | 每日上午10点快速浏览关键指标，识别重点产品/重点车间偏差，发起专项对标 | 告警阈值、对比维度、行业对标位置、归因合理性 |
| **P3 车间主任 / 工艺工程师** | 对标分析、RPA整改 | 接收到RPA整改任务后，查看AI归因的原材料损耗/人工效率差异，落实整改 | 差异分解粒度、任务优先级、执行进度追踪、整改闭环确认 |
| **P4 GMP合规专员** | 智能报告（审计） | 抽查报告数据溯源，验证分析结论与原始数据一致性，确认文档编号与受控标识 | 数据来源标注、版本号、受控文件章、变更留痕 |

### 2.2 典型使用流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 打开系统  │───▶│ 选择产品/ │───▶│ 浏览看板  │───▶│ 发现异常  │
│ (首页)    │    │ 会计月份  │    │ (8维度)   │    │ (告警条)  │
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                      ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 派发RPA  │◀───│ 下载报告  │◀───│ 一键生成  │◀───│ 对标分析  │
│ 整改任务  │    │ (Word)   │    │ 完整报告  │    │ (3步走)   │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

---

## 3. 信息架构与页面结构

### 3.1 站点地图（Sitemap）

```
成本智能分析平台 /
├── 顶部控制栏（全局）
│   ├── Logo + 系统名称 + 版本号
│   ├── 主导航（4个模块Tab）
│   └── 控制室状态（GMP合规标识 + 服务在线 + 实时时钟）
│
├── 面包屑导航（ERP风格）
├── 全局选择器（产品 + 会计月 + 数据截至时间戳）
│
├── 页面容器（SPA切换）
│   ├── 成本看板（#dashboard）
│   ├── 智能报告（#report）
│   ├── 对标分析（#benchmark）
│   └── RPA整改（#rpa）
│
└── 页脚审计信息
    ├── 数据来源说明 + 同步频率
    └── 平台版本 + 文档编号 + 受控文件标识
```

### 3.2 四大核心页面详解

#### 页面1：成本看板（#dashboard）— SCADA/HMI式总览

| 区块 | 内容组成 | 数据刷新 |
|------|---------|---------|
| **告警条** | 超出±10%阈值的KPI波动，左侧4px色条区分上升/下降 | 切换产品/月份时刷新 |
| **指标卡行**（SCADA面板） | 4个工业HMI风格渐变卡：当月产量、单位成本、直接材料、直接人工/制造费用；含环比标签（↑/↓/→） | 同上 |
| **三维成本对比表** | 当月vs上月vs去年同期vs本月预算的7列对比，含环比/同比/预算偏差率 | 同上 |
| **成本趋势图**（ECharts） | 12个月单位成本折线，多系列叠加（材料/人工/制造费用） | 同上 |
| **成本结构图**（ECharts） | 当月成本结构饼图（环形）+ vs上月对比 | 同上 |
| **成本瀑布图**（ECharts） | 从去年同期到当月的因素贡献瀑布分解 | 同上 |
| **原材料TOP消耗表** | TOP10原材料单价/用量/金额对比，前三名高亮渐变条 | 同上 |
| **制造费用明细表** | 费用项分栏对比，合计行加粗蓝色底 | 同上 |
| **人工工时指标** | 单位人工成本 + 万件工时 + 平均时薪，三栏并排对比 | 同上 |
| **行业成本对标条** | 本企业 vs 行业P25/P50/P75 vs 标杆企业，横向条形对照 | 同上 |

#### 页面2：智能报告（#report）— 文档生成控制台

| 区块 | 状态 | 说明 |
|------|------|------|
| **报告配置卡** | 常驻 | 3个下拉 + 1个主按钮：选择产品（▎前缀）、选择月份、选择报告类型（月度/季度/年度）、生成报告（大号主按钮） |
| **进度条区域** | 生成中显示 | 4步流程指示器：数据采集 → 成本分析 → AI撰写 → 报告排版；每步完成变蓝，当前步高亮，进度条百分比填充 |
| **报告结果卡** | 完成后显示 | 成功/失败状态图标、产品+月份+用时信息、下载报告Word链接、重试按钮 |
| **报告预览区** | 完成后显示 | 6章结构HTML预览：摘要→成本结构→环比变动→行业对标→改进建议，蓝色标题分级排版 |
| **历史报告列表** | 常驻 | 报告编号/产品/月份/状态/生成时间表格 + 下载操作列 |

#### 页面3：对标分析（#benchmark）— 三步式向导流程

| 步骤 | 标题 | 核心内容 |
|------|------|---------|
| **Step 1 · 差异总览** | 两厂成本差异对比 | 6列对比表：成本项目 / 一厂 / 二厂 / 差异额 / 差异率 / 差异方向。合计行加粗高亮 |
| **Step 2 · 因素贡献** | 因素贡献度分析 | ECharts水平条形图：正向贡献（红，一厂高）/ 负向贡献（绿，一厂低），条形外侧标注百分比 |
| **Step 3 · AI归因** | AI成本差异归因分析 | 关键差异摘要（ul列表）+ AI文本块（边框+左侧色条+头部图标），分4点呈现：原材料差异→人工差异→制造费用→改进建议 |

> 三步之间通过底部"上一步/下一步"按钮切换，顶部有数字步骤指示器和连接线（完成变绿/当前高亮/未到达灰色）。

#### 页面4：RPA整改（#rpa）— 任务调度控制台

| 区块 | 内容 | 状态类型 |
|------|------|---------|
| **RPA统计卡行** | 4卡片：任务总数（蓝）/ 进行中（琥珀）/ 已完成（绿）/ 失败异常（红） | 实时统计 |
| **筛选操作栏** | 4个筛选下拉（状态/优先级/产品/月份）+ 筛选按钮；右侧派发任务区（产品+月份+派发按钮） | 待执行/执行中/已完成/失败；高/中/低优先级 |
| **任务列表** | 8列表格：任务ID（等宽字体）/ 产品 / 月份 / 任务类型 / 状态Badge / 优先级Tag / 创建时间 / 完成时间 | 状态色：待执行-黄、执行中-蓝、已完成-绿、失败-红 |
| **派发结果Toast** | 成功/失败结果卡片，左侧4px色条，5秒自动消失 | 非阻塞式提示 |

---

## 4. 视觉设计规范

### 4.1 色彩系统（GMP工业蓝灰 + 语义色）

#### 主色板（Primary Palette）

| 角色 | HEX | RGB | CMYK（近似） | 使用场景 |
|------|-----|-----|-------------|---------|
| **Primary 主色** | `#24476b` | 36, 71, 107 | 66, 34, 0, 58 | 顶部控制栏、主按钮、表格表头、标签强调 |
| Primary-Light | `#3a6188` | 58, 97, 136 | 57, 29, 0, 47 | 主按钮hover、链接hover |
| Primary-Dark | `#1a3450` | 26, 52, 80 | 67, 35, 0, 69 | 控制栏背景、合计行加粗边线 |
| Primary-50 | `#eef3f8` | 238, 243, 248 | 4, 2, 0, 3 | 表格表头底色、hover行底色 |
| Primary-100 | `#dde7f0` | 221, 231, 240 | 8, 4, 0, 6 | 表格合计行底色、分割线高亮 |

#### 强调色板（Accent Palette - 限制使用）

| 角色 | HEX | RGB | 使用场景 | 限制 |
|------|-----|-----|---------|------|
| **Accent 工业橙** | `#d4610a` | 212, 97, 10 | Logo背景、控制栏底部3px装饰条、卡片标题左侧小竖条、排名标识 | 仅作点缀，占比≤3% |
| Accent-Light | `#ef7c26` | 239, 124, 38 | GMP合规标识边框色、hover强调 | 同上 |
| Accent-Soft | `#fdf1e6` | 253, 241, 230 | Toast/高亮行底色 | 同上 |

#### 语义色板（Semantic / 药品生产色系）

| 角色 | HEX | RGB | 对应GMP术语 | 使用场景 |
|------|-----|-----|------------|---------|
| **Success 合格绿** | `#2e7d32` | 46, 125, 50 | Pass / 合格 | 完成状态、低于阈值下降、服务在线指示灯 |
| Success-Soft | `#e8f5e9` | 232, 245, 233 | — | 成功类告警条底色、成功Badge底色 |
| **Danger 偏差红** | `#c62828` | 198, 40, 40 | Deviation / 偏差 | 超标上升、失败状态、任务异常 |
| Danger-Soft | `#ffebee` | 255, 235, 238 | — | 偏差类告警条底色、失败Badge底色 |
| **Warning 预警橙** | `#f57f17` | 245, 127, 23 | OOS / 预警 | 中间优先级、待执行状态 |
| Warning-Soft | `#fff3e0` | 255, 243, 224 | — | 待执行Badge底色 |
| **Info 信息蓝** | `#1565c0` | 21, 101, 192 | Information | 信息提示类告警条、执行中Badge |
| Info-Soft | `#e3f2fd` | 227, 242, 253 | — | 信息类背景 |

#### 中性色板（Neutral / 工业灰阶）

| 角色 | HEX | RGB | 使用场景 |
|------|-----|-----|---------|
| **BG 页面背景** | `#eef1f5` | 238, 241, 245 | 全局背景 + 20px方格纹理（透明度2.5%蓝灰） |
| Card-BG 卡片背景 | `#ffffff` | 255, 255, 255 | 所有卡片、表格、容器 |
| Card-BG-Alt | `#f7f9fb` | 247, 249, 251 | 斑马纹表格偶数行、交替背景区 |
| **Text-Primary** | `#1f2937` | 31, 41, 55 | 正文标题、表格内容、按钮文字 |
| Text-Secondary | `#4b5563` | 75, 85, 99 | 下拉标签、次要说明、筛选条件标签 |
| Text-Muted | `#6b7280` | 107, 114, 128 | 面包屑、占位符、辅助文字 |
| **Border 分割线** | `#d6dce3` | 214, 220, 227 | 卡片边框、表格单元格边框、常规分割 |
| Border-Strong | `#b8c1cc` | 184, 193, 204 | 下拉默认边框、滚动条滑块、hover分割线 |

> **⚠️ 禁止项**：禁止使用任何紫色系（#7c3aed 等）、粉色系（#ec4899 等）、亮青色系（#06b6d4 等）。禁止在任何组件上使用 `linear-gradient` 装饰色块（指标卡渐变背景除外，限定蓝灰/绿/橙/红工业色）。

### 4.2 排版系统（Typography）

#### 字体栈（无外部字体依赖）

```css
/* 中文字体：系统原生优先，无Google Fonts请求 */
font-family: 'Microsoft YaHei'   /* Win 优先 */
             'PingFang SC'      /* macOS 优先 */
             'Source Han Sans CN',
             'Hiragino Sans GB',
             'SimHei',           /* Win 旧版兜底 */
             sans-serif;

/* 等宽数字（金额/百分比/ID） */
font-family: 'Consolas',
             'SF Mono',
             'Cascadia Mono',
             'Microsoft YaHei Mono',
             monospace;
```

#### 字号层级表（最小12px，无装饰性超大字）

| 层级 | 字号（px） | 字重 | 行高 | 字距 | 使用场景 |
|------|----------|------|------|------|---------|
| H1 系统标题 | 17 | 700 | 1.2 | 2px | 顶部系统名称"成本智能分析平台" |
| H2 页面标题 | 18 | 700 | 1.3 | 0.5px | 4个页面的主标题（如"成本对标分析"） |
| H3 卡片标题 | 13.5 | 700 | 1.4 | 0.2px | 所有 `.card-title` |
| Body 正文 | 14 | 400 | 1.5 | 0 | 段落正文、按钮默认、普通说明 |
| Small 小字号 | 12.5 | 500 | 1.4 | — | 表格内容、下拉选项、筛选标签 |
| Caption 注释 | 11.5 | 500 | 1.3 | 0.8px | 指标卡标签、版本号、GMP小字、页脚 |
| Tiny 标签 | 10-11 | 700 | 1 | 1px | GMP认证章、Badge内文字、单位标签 |
| **数字大字** | 28 | 700 | 1.2 | -0.4px | 指标卡数值（等宽 tabular-nums） |

### 4.3 间距与栅格系统

#### 栅格（Grid System）
- 内容区最大宽度：`1560px`
- 内容区左右内边距：`20px`
- 指标卡行：`grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))`，`gap: 12px`
- 图表行2列：`grid-template-columns: 1fr 1fr; gap: 12px`
- 图表行3列：`grid-template-columns: 1fr 1fr 1fr; gap: 12px`

#### 间距刻度（8-4-12系统）

| 刻度 | 值（px） | 使用场景 |
|------|---------|---------|
| xs | 4 | 图标与文字间距（gap）、Badge内边距、极细分割 |
| sm | 6-8 | 按钮内边距（竖8横18）、卡片标题下margin |
| md | 10-12 | 卡片内边距（16×18）、表单元格（8×12）、行间距 |
| lg | 14-16 | 全局选择器padding、卡片之间margin-top、指标卡padding |
| xl | 20-28 | 顶部控制栏左右padding（28）、主内容区左右padding（20） |

### 4.4 圆角与边框

| 类型 | 半径值 | 使用对象 |
|------|--------|---------|
| 无圆角 | 0px / 2px | 工业按钮 `border-radius:2px`、下拉框、告警条左侧色条、GMP标识章 |
| **标准小圆角** | **4px** | 指标卡、常规卡片（默认`--radius`） |
| 大圆角 | 6-8px | 卡片容器（`.card { border-radius:6px }`）、图表容器 |
| **禁止** | >8px / 50% | ❌ 胶囊按钮、圆形头像、全圆角卡片 |

---

## 5. 交互设计规范

### 5.1 导航交互

| 交互类型 | 行为 | 动效参数 |
|---------|------|---------|
| 主导航Tab点击 | 点击后切换`.active`类（下边框工业橙3px + 深色背景 + 白字），SPA切换页面容器 | 颜色+背景过渡 `0.12s linear`，**无滑入动画** |
| 面包屑跳转 | 点击"成本分析"返回看板首页 | 即时跳转，无过渡 |
| 全局选择器变更 | 切换产品/月份后，4个页面内所有数据重新加载，首屏显示加载骨架 | 数据加载`inline-loading`，**无骨架屏闪动** |

### 5.2 反馈交互

#### 按钮状态反馈

| 状态 | 视觉表现 | 触发方式 |
|------|---------|---------|
| Default | 主色填充/描边 | 初始 |
| Hover | 主色变亮一级（`--primary → --primary-light`），描边变深 | 鼠标悬停 `0.1s` 响应 |
| Active/Click | 无位移、无缩放（工业按钮克制） | 鼠标按下 |
| Disabled | 灰色填充（`--border-strong`），灰色文字，`cursor:not-allowed` | `disabled`属性 |
| Loading | 按钮内左侧插入`spinner-small`（16px方形边框旋转动画），文字变"生成中..." | 异步请求期间 |

#### 加载反馈

| 类型 | 视觉 | 场景 |
|------|------|------|
| **全局加载遮罩** | 半透明工业灰背景 + 40px方形旋转器（非圆形！steps(8)阶梯动画） + 文字"数据加载中..." | 首次进入页面、知识库构建大任务 |
| **行内加载** | 16px小方形旋转器 + 灰色文字"加载XX中..." | 单卡片内数据加载、表格加载 |
| **步骤加载** | 进度条填充（工业条，无光泽）+ 4步文字指示器完成态高亮 | 报告生成流程 |

#### Toast / 提示反馈

| 类型 | 视觉 | 消失规则 |
|------|------|---------|
| 成功Toast | 卡片样式 + 左侧4px绿色竖条 + SVG图标 + 成功文字 | 5秒自动淡出，无残留 |
| 失败Toast | 卡片样式 + 左侧4px红色竖条 + SVG图标 + 失败文字 | 5秒自动淡出，无残留 |
| 告警条 | 页面顶部扁平条，4px左侧色条（红/蓝/橙），SVG告警图标 + 文字 | 常驻，页面刷新后重新计算 |

### 5.3 表格交互

| 交互 | 行为 |
|------|------|
| 行hover | 整行底色变为`--primary-50`，**无高亮发光** |
| 表头排序可点击 | 光标pointer，hover加深表头底色，排序箭头`⇅ → ▲/▼` |
| 金额列对齐 | 全部右对齐，`tabular-nums`等宽数字字体 |
| 正负值染色 | 正值（不利差异）红/深，负值（有利差异）绿/深 |
| Top3高亮 | 前三名行左侧渐变琥珀色背景条，不影响文字对比 |
| 合计行 | 独立底色`--primary-100`，加粗字体，顶部2px粗边线 |

### 5.4 动效标准（克制主义）

| 动效 | 时长 | 曲线 | 允许场景 |
|------|------|------|---------|
| 颜色过渡 | 100-120ms | linear | 按钮hover、导航Tab、表格行hover |
| 加载旋转 | 0.8-0.9s | `steps(6/8)` 阶梯 | 方形spinner（禁止smooth圆形loading） |
| 进度条填充 | 即时/线性 | linear | 报告生成进度，无弹性动画 |
| **禁止** | — | — | ❌ 页面切换fade/slide、卡片入场fade-up、数字countUp、弹窗bounce、毛玻璃backdrop-blur |

---

## 6. 组件库规范（状态矩阵）

### 6.1 按钮组件（`.btn`）

| 变体 | 类名 | 背景 | 边框 | 文字色 | Hover变化 | 尺寸选项 |
|------|------|------|------|--------|----------|---------|
| **主按钮** | `.btn-primary` | `--primary #24476b` | `--primary-dark` | `#fff` | 背景→`--primary-light` | `.btn-sm`(5×12/12px) / 默认(8×18/13px) / `.btn-lg`(11×26/14px) |
| 成功按钮 | `.btn-success` | `--success #2e7d32` | `#1b5e20` | `#fff` | 背景→`#388e3c` | 同左 |
| **描边按钮** | `.btn-outline` | `#fff` | `--border-strong` | `--primary` | 背景→`--primary-50`，边框→`--primary-light` | 同左 |
| 禁用态 | `:disabled` | `--border-strong` | `--border-strong` | `--text-muted` | — | — |

### 6.2 下拉选择器（`.selector`）

| 属性 | 值 | 说明 |
|------|-----|------|
| 高度 | 32px（7px上下内边距） | ERP标准高度 |
| 最小宽度 | 160px | 防止内容截断 |
| 边框色 | 默认`--border-strong`，hover`--primary-light`，focus`--primary`外描边1px | 工业描边风格 |
| 下拉箭头 | 原生`appearance:none` + SVG方头箭头内嵌（`stroke-linecap:square`） | **禁止系统默认圆形箭头** |
| 标签前缀 | `▎`字符（U+258E左半块）+ 标签文字 | ERP下拉风格 |

### 6.3 徽章/标签组件（Badge）

| 类型 | 类名 | 背景 | 边框/字色 | 使用 |
|------|------|------|----------|------|
| 成功 | `.badge-success` | `--success-soft` | 字色`--success` | 报告完成、任务done |
| 信息 | `.badge-info` | `--info-soft` | 字色`--info` | RPA执行中状态 |
| 警告 | `.badge-warning` | `--warning-soft` | 字色`--warning` | 待执行状态 |
| 危险 | `.badge-danger` | `--danger-soft` | 字色`--danger` | 失败状态 |
| 优先级高 | `.task-priority-high` | `--danger-soft` | 字色`--danger` | RPA高优先级标签 |
| 优先级中 | `.task-priority-medium` | `--warning-soft` | 字色`--warning` | RPA中优先级标签 |
| 优先级低 | `.task-priority-low` | `--primary-50` | 字色`--primary` | RPA低优先级标签 |

### 6.4 表格组件（`.data-table`）

| 结构类 | 样式规则 |
|--------|---------|
| `.data-table-wrapper` | 横滚容器，1px边框外框 |
| `th`（表头） | `--primary-50`底色、`--primary-dark`字色、加粗、底部2px粗边、sticky吸顶 |
| `td`（单元格） | 8×12内边距、底部1px细线分割 |
| `tr:nth-child(even)` | 斑马纹`--card-bg-alt` |
| `tr:hover` | 行底色`--primary-50` |
| `tr.highlight` | 琥珀色高亮渐变，用于重点行 |
| `tr.total-row` | 蓝灰底+加粗+顶部2px粗边线，合计行 |
| `.text-right` | 右对齐 + tabular等宽数字字体 |
| `.positive / .negative` | 差异染色（红/绿），加粗700字重 |

### 6.5 指标卡组件（SCADA HMI风格）

| 属性 | 值 | 说明 |
|------|-----|------|
| 尺寸 | 最小200px宽度，自适应栅格 | 可容纳1-6个并排 |
| 渐变背景 | 从`--primary-dark`→`--primary`线性渐变（180deg） | 工业蓝灰HMI面板 |
| 变体 | `.accent`(琥珀渐变)/`.success`(绿渐变)/`.danger`(红渐变)/`.warning`(橙渐变) | 对应不同语义的统计卡 |
| 标签（上） | 11.5px + 72%透明白字 + 字距0.8px | 如"当月产量" |
| 数值（中） | **28px加粗 + Consolas等宽 + tabular-nums** | 核心视觉焦点 |
| 单位（右上） | `::after`伪元素+data-unit属性，10px浅灰透明白字 | 如"元/盒" |
| 环比标签（下） | 12px等宽字体+2×8内边距小方块+2px圆角，up红/down绿/flat灰 | 如"↑ +5.2%" |

### 6.6 告警条组件（`.alert-item`）

| 变体 | 类名 | 背景色 | 左侧4px色条 | 字色 |
|------|------|--------|------------|------|
| 上升告警（不利） | `.alert-up` | `--danger-soft #ffebee` | `--danger #c62828` | `#8b1f1f`深红字 |
| 下降提示（有利） | `.alert-down` | `--info-soft #e3f2fd` | `--info #1565c0` | `#0d3d7a`深蓝字 |

---

## 7. 响应式与性能要求

### 7.1 断点与适配策略

由于制药企业ERP系统**固定在办公桌面显示器使用**，本系统采用**有限响应式**策略：

| 断点范围 | 适配策略 | 最小允许宽度 |
|---------|---------|------------|
| ≥1560px（主流23寸+） | 内容区居中1560px，两侧留白对称 | — |
| 1280px ~ 1560px（19-22寸） | 内容区全宽自适应，图表栅格自动收缩 | **支持** |
| 1024px ~ 1280px（老显示器/笔记本） | 强制出现水平滚动条，表格/图表保持最小宽度 | **支持（需横滚）** |
| <1024px（平板/手机） | ❌ 不做移动端适配，保留`body { min-width:1280px }`强制出现滚动条 | **不支持** |

> **设计约束**：本系统不做移动端适配，不做汉堡菜单折叠，不做卡片流单列化。

### 7.2 性能要求

| 指标 | 要求 | 测试方法 |
|------|------|---------|
| 首屏加载（HTML+CSS+JS） | ≤1.2s（本地环境），≤2.5s（内网） | Chrome DevTools Network面板 |
| ECharts图表渲染 | 单图≤200ms，全页≤1.2s | Performance面板 |
| 页面切换（SPA Hash路由） | ≤300ms完成DOM替换 | 肉眼+Console计时 |
| 表格500行渲染 | ≤500ms无卡顿 | 大数据量测试 |
| SVG图标加载 | 0s（纯DOM内联，无HTTP请求） | 禁止<img src="svg">形式 |
| 总首屏请求数 | ≤12个（页面1+CSS1+ECharts CDN1+5×JS + favicon等） | Network统计 |
| 首屏总体积（gzip） | ≤350KB（ECharts约280KB） | Network统计 |

---

## 8. 无障碍与可访问性设计

| 维度 | 要求 | 实现方式 |
|------|------|---------|
| **色彩对比度** | 正文文字≥4.5:1（WCAG AA级），大字≥3:1 | `--text-primary #1f2937` on `#fff` = 15.2:1（符合） |
| 色盲友好 | 红绿差异不依赖单一色彩通道 | 表格正负值同时用红色/绿色+字重加粗+数值前`+/-`号辅助识别 |
| 焦点可见 | 所有可交互元素有明显焦点环 | `.selector:focus { outline:1px solid --primary; outline-offset:-1px }` |
| 语义化标签 | 页头/导航/主内容/页脚采用`<header><nav><main><footer>` | index.html已实现 |
| 图标可理解 | 所有装饰性SVG配文字说明，按钮采用"图标+文字"双标识 | 禁止单独图标按钮无tooltip/文字 |
| 键盘导航 | Tab顺序=DOM顺序，Enter/Space触发按钮 | 原生HTML表单+链接支持 |
| 动效减弱 | `prefers-reduced-motion: reduce`下禁用spinner动画和过渡 | 预留CSS媒体查询实现 |

---

## 9. 图标系统规范

### 9.1 实现方式

**纯内联SVG + JavaScript Icon Library（`window.Icons`对象），零外部依赖，禁止使用：**
- ❌ emoji表情符号（⚖️📊🤖⚡✅❌等，DOM检测需`hasEmoji:false`）
- ❌ FontAwesome / Iconfont 等外部图标字体CDN
- ❌ `<img src="xxx.svg">` 形式（增加HTTP请求）
- ❌ PNG/JPG等位图图标

### 9.2 SVG统一规格

| 参数 | 规范值 |
|------|--------|
| viewBox | `0 0 24 24`（标准24px网格） |
| 风格 | **工业线条风**：stroke描边为主，极少fill，stroke-linecap=square/round、stroke-linejoin=round |
| stroke-width | 2px（默认），card标题图标中保持 |
| 填充色 | `currentColor`继承父元素颜色，便于hover/active态变色 |
| 尺寸接口 | `Icons._svg(content, size)` 可传入自定义大小，默认14px |
| 抗伸缩 | `style="flex-shrink:0"` 防止flex布局内压扁 |

### 9.3 图标清单（共19个 + 导航4个）

#### 卡片/数据类图标

| 图标函数 | SVG代码要点 | 用途 |
|---------|------------|------|
| `Icons.table()` | 矩形+横竖双分割线 | 表格类卡片标题、任务列表 |
| `Icons.chart()` | 3根不等高柱子+基线 | 柱状图卡片标题、贡献度分析 |
| `Icons.pie()` | 圆+四分之一扇形 | 饼图卡片标题、成本结构 |
| `Icons.waterfall()` | 5根错位竖线+基线 | 瀑布图卡片标题、因素分解 |
| `Icons.material()` | 三层平行四边形 | 原材料消耗卡片标题 |
| `Icons.factory()` | 厂房三角屋顶+3根烟囱 | 制造费用卡片标题、一厂/二厂 |
| `Icons.labor()` | 圆头人形+肩膀横线 | 人工工时卡片标题 |
| `Icons.bench()` | 2根不等高柱子+色填充块 | 对标分析卡片标题、指标卡标签 |

#### 状态/告警类图标

| 图标函数 | 用途 |
|---------|------|
| `Icons.warn()` | 三角形感叹号 — 告警条、失败提示、兜底AI归因提示 |
| `Icons.down()` | 向下箭头 — 下降/有利差异标识 |
| `Icons.ok()` | 勾选圆圈 — 成功Toast、报告完成状态、通过标识 |
| `Icons.close()` | X叉号 — 失败Toast、关闭/取消按钮 |

#### 动作/操作类图标

| 图标函数 | 用途 |
|---------|------|
| `Icons.report()` | 折角文件 + 双横线 — 报告页面标题、报告生成 |
| `Icons.gear()` | 齿轮（6齿+中心圆） — 配置卡片标题、设置 |
| `Icons.write()` | 铅笔 — 生成报告按钮、编辑 |
| `Icons.eye()` | 眼睛 — 报告预览标题、查看 |
| `Icons.history()` | 逆时针箭头 + 时钟点 — 历史报告列表标题 |
| `Icons.download()` | 向下箭头 + 硬盘托 — 下载报告链接 |
| `Icons.search()` | 放大镜 — 开始分析按钮、检索 |
| `Icons.scale()` | 天平/标尺 — 对标分析页标题（替代⚖️emoji） |
| `Icons.robot()` | 矩形机器人+双眼点 — RPA页面标题、AI归因（替代🤖emoji） |
| `Icons.bolt()` | 闪电多边形 — 派发任务按钮（替代⚡emoji） |

#### 空状态/排名图标

| 图标函数 | 用途 |
|---------|------|
| `Icons.empty()` | 带勾的文档图标（44px大尺寸） — 空状态页主图 |
| `Icons.emptysm()` | 同上（40px尺寸） — Utils.emptyState()内调用 |
| `Icons.trophy()` | CSS clip-path五角星 + 琥珀条纹 — 排名Top3标识（替代🏆emoji） |

#### 导航图标（index.html内联SVG）

| 位置 | SVG图形特征 |
|------|------------|
| 成本看板 | 2×2不等尺寸矩形4格（Dashboard经典） |
| 智能报告 | 折角文件 + 横线文字（同Icons.report） |
| 对标分析 | 高低双柱 + 填充半透明块（同Icons.bench） |
| RPA整改 | 方头机器人 + 双眼（同Icons.robot） |

---

## 10. 前端技术与实现约束

### 10.1 技术栈（固定，不引入额外框架）

| 层次 | 选型 | 版本 | 约束 |
|------|------|------|------|
| 框架 | 原生HTML + CSS + JavaScript（SPA Hash路由） | ES6+ | ❌ 禁止Vue/React/Angular（减小包体积） |
| 图表库 | ECharts | **5.5.0（CDN）** | jsDelivr固定版本CDN，不可升级不可替换 |
| 构建 | 无构建步骤 | — | ❌ 禁止Webpack/Vite打包，原生文件直接由FastAPI静态托管 |
| 图标 | 内联SVG + Icons对象 | — | 见第9章 |
| 后端集成 | FastAPI + `/static/`目录托管 | — | 静态文件路径`app/static/{css,js,index.html}` |

### 10.2 代码组织

```
app/static/
├── index.html              # 单入口，包含顶部栏+面包屑+全局选择器+页容器+页脚
├── css/
│   └── style.css           # 单文件全部样式（不拆分，减少请求）
└── js/
    ├── app.js              # 全局对象：Router / Utils / Icons / SystemUI（时钟+面包屑）
    ├── dashboard.js        # 成本看板页面 render/chart渲染
    ├── report.js           # 智能报告页面 render/生成流程控制
    ├── benchmark.js        # 对标分析3步向导页面
    └── rpa.js              # RPA任务管理页面
```

### 10.3 后端API回退机制（必须实现）

由于LLM API Key、RPA服务、知识库等依赖可能不可用，**所有前端API调用必须具备模拟数据回退**，保证UI在无后端完全响应的情况下也能展示完整Demo。

| API端点 | 回退策略 | 回退数据存放 |
|---------|---------|------------|
| `/api/dashboard/*` | `try / catch(e)` → 调用本地`_mockXxx()`函数返回模拟结构数据 | 对应页面JS文件末尾 |
| `/api/report/*` | 生成失败 → `simulateProgress()`定时器模拟4步进度 → 完成后渲染模拟预览内容 | `report.js::simulateProgress()` |
| `/api/benchmark/*` | 真实API失败 → 返回硬编码的一厂/二厂差异mock数据 | `benchmark.js::_mockDiff()` 等 |
| `/api/rpa/*` | stats/tasks真实失败 → mock数据；派发失败 → "模拟成功"Toast | `rpa.js::_mockTasks()` |

> 回退时Console打印`[API Error] ...`用于调试，但**不得弹出alert干扰用户**。

---

## 11. 设计交付物清单

### 11.1 已交付（代码实现）

| 交付物 | 文件位置 | 状态 |
|-------|---------|------|
| 单页HTML入口 | [index.html](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/index.html) | ✅ 已交付 |
| GMP风格样式表 | [style.css](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/css/style.css) | ✅ 已交付 |
| 全局JS（路由+工具+图标库+时钟） | [app.js](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/js/app.js#L170-L207) | ✅ 已交付 |
| 成本看板页面 | [dashboard.js](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/js/dashboard.js) | ✅ 已交付 |
| 智能报告页面 | [report.js](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/js/report.js) | ✅ 已交付 |
| 对标分析页面 | [benchmark.js](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/js/benchmark.js) | ✅ 已交付 |
| RPA整改页面 | [rpa.js](file:///c:/Users/Administrator/Desktop/AI大赛/PharmaCost-AI/app/static/js/rpa.js) | ✅ 已交付 |
| 页面预览地址 | http://127.0.0.1:8000/ | ✅ 可访问 |

### 11.2 本文档

| 章节 | 完成度 |
|------|-------|
| 1. 项目概述与目标（含核心原则+量化指标） | ✅ 100% |
| 2. 用户角色与场景分析（4角色+典型流程图） | ✅ 100% |
| 3. 信息架构与页面结构（站点地图+4页面明细） | ✅ 100% |
| 4. 视觉设计规范（色彩×5板 + 排版×8层级 + 间距×5刻度 + 圆角表） | ✅ 100% |
| 5. 交互设计规范（导航×4类+反馈×3类+表格+动效标准） | ✅ 100% |
| 6. 组件库状态矩阵（6大核心组件+变体参数表） | ✅ 100% |
| 7. 响应式与性能（有限响应策略+7项指标） | ✅ 100% |
| 8. 无障碍设计（7维度WCAG AA要求） | ✅ 100% |
| 9. 图标系统（规范+19+4完整清单） | ✅ 100% |
| 10. 前端技术约束（栈+代码组织+API回退机制） | ✅ 100% |
| 11. 交付物清单 | ✅ 100% |

### 11.3 图标下载清单

**无需下载任何图标资源。** 所有图标均为原生内联SVG，已打包在 `app.js:170-207` 的 `Icons` 对象中，代码可直接复用。如确需导出为独立SVG文件用于其他物料，请提出具体需求另行生成。

---

**文档结束 · 受控文件编号 QRM-COP-031 · PharmaCost-AI v2.6**
